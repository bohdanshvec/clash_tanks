# AGENTS.md

## 1. Проект

Учебный проект — браузерная пошаговая танковая карточная стратегия,
вдохновлённая WoT: Generals.

Проект не является копией оригинальной игры. Планируется использовать
собственные названия, карты, изображения и игровые материалы.

Основной стек:

- Ruby 3.4.10
- RubyGems 3.6.9
- Rails 8.1.3.1
- RVM 1.29.12
- Ubuntu 22.04
- Hotwire
- Stimulus
- Turbo
- PostgreSQL 17.x — целевая версия нового проекта
- PostgreSQL 12 и 14 в системе не изменять и не удалять
- PostgreSQL 17 установлен на порту 5434

Проект:

clash_tanks

Локальный запуск:

bin/dev


## 2. Основная идея игры

Игрок управляет армией на игровом поле, использует карты,
перемещает подразделения, атакует противника и выполняет игровые действия
по правилам пошаговой стратегии.

Основная игровая сущность — КАРТА.

Карта может иметь разное назначение.

Основные типы карт:

1. Техника
   - танк или другое боевое подразделение;
   - имеет характеристики;
   - может находиться на игровом поле;
   - может перемещаться и атаковать.

2. Приказ
   - игровое действие или эффект;
   - может изменять состояние игры согласно правилам;
   - может воздействовать на технику, штаб или другие игровые объекты.

3. Взвод
   - вспомогательное подразделение;
   - может усиливать штаб;
   - например, добавлять огневую мощь или HP;
   - конкретные механики будут определены позднее.

Система должна позволять добавлять новые типы карт без
переписывания игрового движка.

Игра разрабатывается постепенно — сначала минимальная рабочая версия,
затем дополнительные игровые механики.


## 3. Главный архитектурный принцип

Игровой движок Rails не должен зависеть от конкретного источника
решений игрока.

Источник решения должен быть абстрагирован.

Возможные источники:

1. Человек-игрок.
2. Локальная AI-модель.
3. Внешняя AI-модель через API.
4. В дальнейшем — другие варианты.

Общая схема:

Game
  ↓
Decision Provider
  ↓
Action
  ↓
Game Engine
  ↓
проверка правил
  ↓
изменение состояния игры


Rails является главным арбитром игры.

AI не имеет права непосредственно изменять состояние игры.

AI только предлагает Action.

Rails проверяет предложенное действие и только после успешной проверки
применяет его.


## 4. Game Engine

Game Engine является центральным компонентом игры.

Именно Game Engine отвечает за:

- правила игры;
- проверку допустимости действий;
- порядок ходов;
- перемещение;
- атаки;
- расчёт урона;
- использование карт;
- изменение ресурсов;
- изменение состояния подразделений;
- победу и поражение;
- другие игровые механики.

Контроллеры Rails не должны содержать основную игровую логику.

Stimulus/JavaScript не является источником истины игровых правил.

Браузер может отображать доступные действия и отправлять их
на сервер, но окончательное решение о допустимости действия
принадлежит Game Engine.


## 5. Game State

Текущее состояние партии должно быть представлено отдельным
понятием GameState.

GameState может содержать, например:

- текущего игрока;
- состояние игроков;
- состояние игрового поля;
- расположение техники;
- состояние штабов;
- HP;
- ресурсы;
- карты;
- карты на руках;
- активные эффекты;
- номер хода;
- другую необходимую информацию.

Game Engine работает с GameState.

GameState не должен быть привязан непосредственно к HTTP-запросу.

PostgreSQL используется для хранения постоянных данных и состояния
активных партий, а также при необходимости истории ходов.

Не следует записывать в PostgreSQL каждое промежуточное изменение
состояния отдельно, если это не требуется архитектурой игры.


## 6. Game / Player / GamePlayer

Основные модели первого этапа:

Game
- конкретная игровая партия;
- состояние партии;
- статус;
- текущий игрок;
- другие постоянные данные партии.

Player
- участник игры;
- может быть человеком или AI.

GamePlayer
- связь Player с конкретной Game;
- позволяет одному Player участвовать в нескольких партиях;
- хранит данные участника конкретной партии.

Игра не должна различать Human Player и AI Player на уровне
основных правил.

Оба должны использовать одинаковый механизм действий.


## 7. Action

Любое игровое действие должно представляться как Action.

Примеры:

- Move
- Attack
- PlayCard
- EndTurn
- другие действия, которые будут добавляться позднее.

Общая схема:

Player
  ↓
Action
  ↓
Game Engine
  ↓
validate
  ↓
apply
  ↓
Result


Game Engine должен проверять:

- кто выполняет действие;
- чей сейчас ход;
- существует ли объект;
- принадлежит ли объект игроку;
- допустимо ли действие;
- хватает ли ресурсов;
- соблюдены ли остальные правила игры.

Только после успешной проверки действие применяется.


## 8. Result

Результат выполнения Action должен быть отделён от самого Action.

Result может содержать информацию о:

- успешности действия;
- изменениях состояния;
- ошибке;
- причине отказа;
- событиях, которые произошли в результате действия.

Конкретная структура Result будет определена при реализации Game Engine.


## 9. AI-противник

Первоначально планируется использовать:

Qwen3 1.7B

Запуск:

Ollama + Qwen3 1.7B

Модель должна работать локально на компьютере разработчика.

Ориентир по времени ответа AI:

до ~10 секунд.

В дальнейшем модель можно заменить без изменения игрового движка.


## 10. Взаимодействие с AI

AI получает необходимую информацию для принятия решения:

- правила игры;
- текущее состояние игры;
- состояние игрового поля;
- доступные карты;
- ресурсы;
- историю предыдущих ходов;
- список допустимых действий.

AI возвращает выбранное действие.

Предпочтительный формат ответа — структурированный ответ.

Например:

{
  "action_id": 17
}

или другой согласованный формат Action.


AI не должен самостоятельно изменять:

- здоровье подразделений;
- положение объектов;
- ресурсы;
- состояние поля;
- карты;
- другие данные игры.

Все изменения выполняются Game Engine.


## 11. Допустимые действия

Game Engine должен уметь определить список действий,
которые допустимы в текущем состоянии игры.

Decision Provider получает этот список.

AI выбирает одно из допустимых действий.

Это позволяет отделить:

- принятие стратегического решения;
- проверку правил игры.

Даже если AI вернул неправильное или недопустимое действие,
Game Engine должен его отклонить.


## 12. Decision Provider

Источник решений должен быть отделён от Game Engine.

Предварительная концепция:

Decision Provider
    ├── Human
    ├── Ollama
    ├── External API
    └── другие реализации в будущем

Game Engine не должен напрямую зависеть от:

- Ollama;
- Qwen;
- конкретной AI-модели;
- внешнего API.

Game Engine работает только с Action.


## 13. AI Provider

AI Provider отвечает только за получение решения от AI.

Примерная структура:

app/services/ai/

    decision_provider.rb
    ollama/
        client.rb
        decision_provider.rb

Конкретная структура может измениться во время разработки.

AI Provider не имеет права напрямую изменять GameState.


## 14. Предлагаемая структура приложения

Основная структура:

app/
├── controllers/
│   ├── games_controller.rb
│   ├── turns_controller.rb
│   └── ...
│
├── models/
│   ├── game.rb
│   ├── player.rb
│   ├── game_player.rb
│   ├── card.rb
│   └── ...
│
├── services/
│   ├── game_engine/
│   │   ├── engine.rb
│   │   ├── game_state.rb
│   │   ├── action.rb
│   │   ├── result.rb
│   │   ├── turn.rb
│   │   └── rules/
│   │
│   ├── actions/
│   │   └── ...
│   │
│   └── ai/
│       └── ...
│
├── javascript/
│   └── controllers/
│       └── ...
│
└── views/
    ├── games/
    └── ...


Конкретные каталоги и классы создавать по мере необходимости.

Не создавать большое количество пустых каталогов заранее.


## 15. Карты

Карта является общей игровой сущностью.

Не следует архитектурно разделять понятия:

"Карта" и "Танк"

как две независимые игровые сущности верхнего уровня.

Танк является одним из типов карты.

Предварительная концепция:

Card
  ├── Technique
  ├── Order
  └── Platoon

Названия конкретных классов, наследования или другого способа
реализации будут определены при создании карточной системы.

На ранних этапах не следует преждевременно усложнять модель карт.


## 16. Игровое поле

Игровое поле является частью состояния игры.

На поле могут находиться игровые объекты, в первую очередь техника.

Игровое поле должно быть независимо от интерфейса.

Stimulus только отображает состояние поля и отправляет действия.

Правила размещения и перемещения объектов находятся в Game Engine.


## 17. Хранение данных

PostgreSQL используется для:

- игроков;
- партий;
- связей игроков с партиями;
- карт и их постоянных характеристик;
- результатов игр;
- при необходимости истории ходов;
- состояния активных партий.

Быстро изменяющееся игровое состояние не следует хранить
в cookies.

Не использовать cookies как основной механизм хранения
состояния игры.


## 18. Принцип разработки

Разрабатывать постепенно.

Каждый этап должен оставлять проект работоспособным.

Не реализовывать сложные функции заранее без необходимости.

Не создавать архитектуру только ради предполагаемых будущих возможностей,
если она не нужна текущему этапу.

При этом основные архитектурные границы должны сохраняться.


## 19. План реализации

Разработка выполняется в следующем порядке.

### Этап 1 — базовые модели

Создать:

- Game
- Player
- GamePlayer

Цель:

создать игру и добавить двух участников.


### Этап 2 — GameState

Создать представление состояния партии.

Определить:

- игроков;
- текущий ход;
- базовое состояние игры;
- минимальное состояние поля.


### Этап 3 — Game Engine

Создать:

- GameEngine::Engine
- Action
- Result
- базовые правила.

Первое действие:

EndTurn.


### Этап 4 — игровое поле

Создать минимальное поле.

Добавить координаты и правила размещения объектов.


### Этап 5 — карточная система

Создать Card и базовую систему типов карт.

Предварительные типы:

- Technique;
- Order;
- Platoon.

Не реализовывать все характеристики карт сразу.


### Этап 6 — техника

Добавить первую карту типа Technique.

Например, первый танк.

Определить минимальные характеристики:

- HP;
- атака;
- броня;
- стоимость;
- дальность;
- перемещение.

Конкретный набор характеристик может измениться.


### Этап 7 — действия

Добавить:

- Move;
- Attack;
- PlayCard;
- другие необходимые действия.


### Этап 8 — боевые правила

Реализовать:

- проверку дальности;
- атаку;
- расчёт урона;
- HP;
- уничтожение;
- другие базовые правила.


### Этап 9 — ресурсы и ходы

Добавить:

- ресурсы;
- ограничения действий;
- начало хода;
- конец хода;
- условия победы и поражения.


### Этап 10 — базовый интерфейс

Создать интерфейс:

- игрового поля;
- техники;
- карт;
- ресурсов;
- текущего игрока;
- доступных действий.


### Этап 11 — Human vs Human

Сначала должна работать полноценная игра:

Human
vs
Human

Даже если оба игрока управляются из одного браузера.


### Этап 12 — Decision Provider

Ввести абстракцию источника решений.

Создать Human Decision Provider.


### Этап 13 — AI

Создать AI Decision Provider.

Подключить Ollama.


### Этап 14 — Qwen3 1.7B

Подключить Qwen3 1.7B.

AI должен получать состояние игры и допустимые действия
и возвращать Action.


### Этап 15 — AI-тестирование

Проверить, что AI не может:

- нарушить правила;
- ходить вне очереди;
- использовать чужие карты;
- изменять HP напрямую;
- изменять ресурсы напрямую;
- изменять состояние игры напрямую;
- выполнять недопустимые действия.


### Этап 16 — дальнейшее развитие

После создания минимальной рабочей игры:

- новые карты;
- новые типы техники;
- новые приказы;
- новые взводы;
- дополнительные игровые механики;
- балансировка;
- история игр;
- PvP;
- дополнительные AI;
- другие возможности.
 

## 20. Тестирование

Игровые правила должны тестироваться независимо от браузерного интерфейса.

Особое внимание уделять тестированию:

- Game Engine;
- Actions;
- GameState;
- боевых правил;
- проверки допустимости действий;
- победы и поражения;
- AI Actions.

UI не должен быть единственным способом проверки игровой логики.


## 21. Важные архитектурные запреты

Не следует:

- помещать основную игровую логику в Controllers;
- делать JavaScript источником истины игровых правил;
- позволять AI напрямую изменять GameState;
- привязывать Game Engine к Ollama;
- привязывать Game Engine к Qwen;
- хранить основное состояние игры в cookies;
- делать PostgreSQL механизмом записи каждого промежуточного
  изменения состояния;
- создавать сложные игровые механики до необходимости;
- копировать архитектуру старого Tetris-проекта без проверки
  её соответствия Clash Tanks.


## 22. Важное правило для будущей работы

## Что уже сделано:

### 19.1. Созданы три модели: 
- Game
		id
		created_at
		updated_at

- Player
		id
		created_at
		updated_at

- GamePlayer
		id
		game_id
		player_id
		created_at
		updated_at
		
### И их взаимосвязь :
	Player
		has_many :game_players
		has_many :games, through: :game_players

	Game
		has_many :game_players
		has_many :players, through: :game_players

	GamePlayer
		belongs_to :game
		belongs_to :player		
		
		

AGENTS.md является актуальным источником информации о проекте.

При изменении архитектуры, технологий или важных решений
AGENTS.md необходимо обновлять.

Старая информация из предыдущих разговоров не должна считаться актуальной,
если она противоречит текущему AGENTS.md.

При внесении изменений в архитектуру необходимо сначала проверить,
не противоречат ли они основным принципам этого файла.
